import calendar

def display_calendar(year, month):
    """Display a calendar for given year and month"""
    print(calendar.month(year, month))

# Example usage:
display_calendar(2023, 9)  # September 2023